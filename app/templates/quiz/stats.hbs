<div class="quizStats">
  <div class="quizStats-winner">
    {{#if game.hasWinner}}
      {{ game.winner.name }} hat gewonnen!
    {{else}}
      Unentschieden!
    {{/if}}
  </div>

  <div class="quizStats-motivation">
    {{#if game.hasWinner}}
      Möchtet ihr es noch mal probieren?
    {{else}}
      Probiert es noch mal, um herauszufinden, wer klüger ist.
    {{/if}}
  </div>

  <div class="quizStats-statistics">
    {{#each game.joinedPlayers as |player|}}
      <p>{{ player.name }} hat <strong>{{ player.correctAnswers }} Fragen</strong> korrekt beantwortet.</p>
    {{/each}}
  </div>

  <div class="quizStats-hint">
    Drückt einen der Buzzer, um ein neues Spiel zu beginnen.
  </div>
</div>

{{player-button-any players=players onselect=(action 'backToIndex')}}

{{after-x-seconds seconds=30 action=(action 'backToIndex')}}
