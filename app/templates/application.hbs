<div class="container">
  <div class="row">
    <div class="col-md-12">
      {{#if currentUser}}
        Hey {{currentUser}}! 
        <button {{action 'logout'}} class="logoutButton"> Logut </button>
        {{outlet}}
      {{else}}
        {{render 'login'}}
      {{/if}}
    </div>
  </div>
</div>
