<p class="playerBox-name">
  {{ player.name }}
</p>

<p class="playerBox-points">
  {{#if player.joined}}
    <span>{{ player.points }}</span> points
  {{/if}}
</p>

<p class="playerBox-instruction">
  {{#if player.joined}}
    {{#unless game.hasStarted}}
      Please wait for other players to join.
    {{else if player.isActive}}
      Wait.
    {{else}}
      Tap the correct button to select an answer.
    {{/unless}}
  {{else}}
    {{#if game.hasStarted}}
      Please wait for the other player to finish this round.
    {{else if game.hasPlayers}}
      Tap any button to join the game.
    {{else}}
      Select a quiz to start a new game!
    {{/if}}
  {{/if}}
</p>
